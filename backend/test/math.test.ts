describe("Simple Math Test", () => {
  test("addition of 2 + 2 equals 4", () => {
    expect(2 + 2).toBe(4);
  });

  test("multiplication of 3 * 3 equals 9", () => {
    expect(3 * 3).toBe(9);
  });

  test("subtraction of 10 - 5 equals 5", () => {
    expect(10 - 5).toBe(5);
  });
});
